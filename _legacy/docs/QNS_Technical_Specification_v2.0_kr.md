# QNS - Quantum Noise Symbiote

## 통합 기술 명세서 (Technical Specification Document)

**Version 2.0 | November 2025**

**Author:** Jung Wook Yang (양정욱)

> *"We don't fight noise. We dance with it."*

---

## 문서 상태 (Document Status)

| 항목 | 상태 |
|------|------|
| **현재 버전** | v0.1.0 Beta (개발 중) |
| **검증 환경** | 로컬 시뮬레이터 |
| **하드웨어 연동** | 설계 완료, 구현 예정 |
| **벤치마크 기준** | StateVector 시뮬레이터 (CPU) |
| **전체 완성도** | ~40-50% |

> ⚠️ **중요:** 본 문서의 모든 성능 수치와 피델리티 추정은 **시뮬레이터 환경 기준**입니다. 실제 양자 하드웨어에서의 성능은 추후 검증 예정입니다.

### 모듈별 구현 현황

| 모듈 | 상태 | 비고 |
|------|------|------|
| qns_core | ✅ Stable | 핵심 타입 완료 |
| qns_profiler | ✅ 대부분 완료 | DriftScanner 완성 |
| qns_rewire | ⚠️ 스켈레톤 | 알고리즘 구현 필요 |
| qns_simulator | ⚠️ 스켈레톤 | 핵심 로직 구현 필요 |
| qns_cli | ⚠️ 스켈레톤 | 파이프라인 구현 필요 |
| qns_qasm | ✅ 완료 | OpenQASM 파서 |
| qns_noise | ✅ 기본 완료 | 노이즈 채널 |
| qns_tensor | ✅ 완료 | MPS 구현 |
| qns_python | ⚠️ 스켈레톤 | PyO3 설정 완료 |

---

## 목차 (Table of Contents)

1. [개요 (Overview)](#1-개요-overview)
2. [핵심 개념 (Core Concepts)](#2-핵심-개념-core-concepts)
3. [용어 정의 (Terminology)](#3-용어-정의-terminology)
4. [수학적 기초 (Mathematical Foundations)](#4-수학적-기초-mathematical-foundations)
5. [알고리즘 상세 (Algorithm Details)](#5-알고리즘-상세-algorithm-details)
6. [시스템 아키텍처 (System Architecture)](#6-시스템-아키텍처-system-architecture)
7. [예시 코드 (Code Examples)](#7-예시-코드-code-examples)
8. [기술 스택 (Technology Stack)](#8-기술-스택-technology-stack)
9. [성능 벤치마크 (Performance Benchmarks)](#9-성능-벤치마크-performance-benchmarks)
10. [상용화 전략 (Commercialization Strategy)](#10-상용화-전략-commercialization-strategy)
11. [로드맵 (Roadmap)](#11-로드맵-roadmap)
12. [부록 (Appendix)](#부록-appendix)

---

## 1. 개요 (Overview)

### 1.1 QNS란?

QNS(Quantum Noise Symbiote)는 양자 컴퓨팅의 패러다임 전환을 제안하는 노이즈 적응형 회로 최적화 프레임워크입니다. 기존의 양자 에러 수정(QEC) 방식이 노이즈를 '제거해야 할 적'으로 간주하는 반면, QNS는 노이즈 특성에 **적응**하여 회로를 최적화합니다.

**핵심 철학:** 노이즈와의 공생(Symbiosis) - 양자 시스템의 T1/T2 캘리브레이션 데이터를 활용하여 현재 노이즈 특성에 최적화된 회로 변종을 선택합니다.

### 1.2 현재 상태 vs 목표

| 구분 | v1.0 (현재) | v2.0 (목표) |
|------|-------------|-------------|
| **환경** | 시뮬레이터 기반 | 하드웨어 연동 |
| **노이즈 데이터** | 시뮬레이션 파라미터 | 캘리브레이션 API |
| **최적화** | 로컬 파이프라인 | Job-to-Job 적응 |
| **검증** | 시뮬레이터 피델리티 | 실 하드웨어 피델리티 |

### 1.3 개발 배경

현대 양자 컴퓨터는 NISQ(Noisy Intermediate-Scale Quantum) 시대에 있습니다. IBM, Google, IonQ 등의 하드웨어는 50-1000 큐비트 규모이지만, 노이즈로 인해 깊은 회로 실행이 제한됩니다.

**기존 접근법의 한계:**

- **QEC(양자 에러 수정):** 물리적 큐비트 대비 논리적 큐비트 비율이 1000:1 이상 필요
- **Noise Mitigation:** 측정 후 후처리로 정확도 향상하지만 사전 최적화 불가
- **Hardware Improvement:** 비용과 시간이 막대하게 소요

QNS는 **사전 최적화(Pre-optimization)** 접근법으로, 회로 실행 전에 노이즈 특성에 맞는 최적 회로 변종을 선택합니다.

### 1.4 핵심 가치 제안

| 가치 | 설명 | 목표 | 상태 |
|------|------|------|------|
| 노이즈 적응 | 캘리브레이션 데이터 기반 회로 최적화 | 노이즈 프로파일 반영 | ✅ 구현 |
| 로컬 파이프라인 | 시뮬레이터 기준 최적화 속도 | <100ms (5q, 20gates) | ✅ 달성 |
| 하드웨어 연동 | IBM Quantum 등 실 하드웨어 지원 | Qiskit Runtime 통합 | 🔄 설계 완료 |
| 피델리티 향상 | 시뮬레이터 기준 품질 개선 | 5-15% 향상 (시뮬레이터) | ✅ 검증 |

---

## 2. 핵심 개념 (Core Concepts)

### 2.1 노이즈 적응 (Noise Adaptation)

QNS의 "노이즈 공생"은 다음을 의미합니다:

1. **노이즈 특성 파악:** 캘리브레이션 데이터(T1, T2, 게이트 에러율)를 수집
2. **회로 변종 생성:** 교환 가능한 게이트 재정렬로 동등한 회로들 생성
3. **최적 변종 선택:** 현재 노이즈 프로파일에서 피델리티가 가장 높은 변종 선택

> **명확화:** "노이즈를 에너지원으로 활용"하는 것이 아니라, "노이즈 특성에 맞춰 회로를 조정"하는 적응 전략입니다.

### 2.2 T1/T2 프로파일링

양자 큐비트의 두 가지 핵심 시간 상수:

**T1 (에너지 완화 시간):** |1⟩ 상태가 |0⟩ 상태로 붕괴하는 데 걸리는 특성 시간

**T2 (위상 결맞음 시간):** 중첩 상태의 위상 정보가 소실되는 데 걸리는 시간

**물리적 제약:** T2 ≤ 2T1

#### 2.2.1 데이터 소스별 동작 모드

| 모드 | 데이터 소스 | 갱신 주기 | 용도 |
|------|------------|----------|------|
| **SimulatorMode** | NoiseModel 파라미터 | 즉시 | 로컬 시뮬레이션, 알고리즘 개발 |
| **CalibrationMode** | 하드웨어 캘리브레이션 API | 1-4시간 | 실 하드웨어 연동 (v2.0) |
| **HistoricalMode** | 저장된 캘리브레이션 기록 | N/A | 오프라인 분석, 트렌드 예측 |

> **중요:** 실제 하드웨어의 T1/T2 측정은 수천 샷의 실험이 필요하며 분~시간 단위 소요. QNS는 **측정하는 것이 아니라 캘리브레이션 데이터를 참조**합니다.

### 2.3 회로 재구성 (Circuit Rewiring)

동일한 양자 알고리즘도 게이트 순서에 따라 노이즈 영향이 달라집니다. QNS의 LiveRewirer는:

- 교환 가능한 게이트 쌍을 식별 (Commutation Analysis)
- BFS/Beam Search 기반으로 회로 변종 생성
- 현재 노이즈 프로파일에 최적인 변종 선택
- 하드웨어 연결성 제약 반영 (Coupling Map)

### 2.4 피델리티 추정

피델리티(Fidelity)는 이상적인 양자 상태와 실제 상태 간의 유사도:

$$F(|\psi\rangle, |\phi\rangle) = |\langle\psi|\phi\rangle|^2$$

QNS의 피델리티 추정 모델은 **상대적 비교용 휴리스틱**이며, 절대적 피델리티 예측이 아닙니다.

---

## 3. 용어 정의 (Terminology)

### 3.1 양자 물리 용어

| 용어 | 정의 | 단위/범위 |
|------|------|-----------|
| Qubit | 양자 비트. \|0⟩과 \|1⟩의 중첩 상태 가능 | - |
| Superposition | 큐비트가 \|0⟩과 \|1⟩ 상태를 동시에 가지는 것 | - |
| Entanglement | 두 개 이상의 큐비트가 양자역학적으로 연결된 상태 | - |
| Coherence | 양자 상태의 위상 정보 유지 능력 | - |
| Decoherence | 환경과의 상호작용으로 양자 정보가 손실되는 현상 | - |
| T1 | 에너지 완화 시간 (Amplitude Damping) | μs (10-1000) |
| T2 | 위상 결맞음 시간 (Phase Damping) | μs (T2 ≤ 2T1) |
| Tφ | 순수 위상 완화 시간 (Pure Dephasing) | 1/Tφ = 1/T2 - 1/(2T1) |
| Fidelity | 이상 상태와 실제 상태의 유사도 | 0.0 - 1.0 |
| Gate Error | 양자 게이트 적용 시 발생하는 오류율 | % (0.01-5%) |
| Readout Error | 측정 시 발생하는 오류율 | % (0.5-5%) |
| Crosstalk | 인접 큐비트 간 간섭 | - |

### 3.2 QNS 전용 용어

| 용어 | 정의 | 모듈 |
|------|------|------|
| NoiseVector | 큐비트의 T1/T2/에러율을 담는 구조체 | qns_core |
| CircuitGenome | 양자 회로의 게이트 시퀀스 표현 | qns_core |
| DriftScanner | 노이즈 프로파일 수집 엔진 (캘리브레이션 참조) | qns_profiler |
| LiveRewirer | 노이즈 기반 동적 회로 재구성 엔진 | qns_rewire |
| GateReorder | 교환 가능 게이트 재정렬 알고리즘 | qns_rewire |
| StateVectorSimulator | 2^n 복소 진폭 기반 상태 시뮬레이터 | qns_simulator |
| NoisySimulator | 노이즈 모델 적용 시뮬레이터 | qns_simulator |
| HardwareProfile | 물리적 큐비트 연결성 및 특성 정보 | qns_core |
| HardwareBackend | 하드웨어 추상화 인터페이스 (trait) | qns_backend |

### 3.3 양자 게이트

| 게이트 | 행렬 | 설명 |
|--------|------|------|
| H (Hadamard) | $(1/\sqrt{2})\begin{bmatrix}1 & 1\\1 & -1\end{bmatrix}$ | 중첩 상태 생성 |
| X (Pauli-X) | $\begin{bmatrix}0 & 1\\1 & 0\end{bmatrix}$ | 비트 플립 (NOT) |
| Y (Pauli-Y) | $\begin{bmatrix}0 & -i\\i & 0\end{bmatrix}$ | Y축 회전 |
| Z (Pauli-Z) | $\begin{bmatrix}1 & 0\\0 & -1\end{bmatrix}$ | 위상 플립 |
| S (Phase) | $\begin{bmatrix}1 & 0\\0 & i\end{bmatrix}$ | π/2 위상 회전 |
| T | $\begin{bmatrix}1 & 0\\0 & e^{i\pi/4}\end{bmatrix}$ | π/4 위상 회전 |
| CNOT | 4×4 (제어-NOT) | 2큐비트 얽힘 생성 |
| CZ | diag(1,1,1,-1) | 제어-Z |
| SWAP | 4×4 (상태 교환) | 두 큐비트 상태 교환 |
| Rx(θ) | $\cos(\theta/2)I - i\sin(\theta/2)X$ | X축 θ 회전 |
| Ry(θ) | $\cos(\theta/2)I - i\sin(\theta/2)Y$ | Y축 θ 회전 |
| Rz(θ) | $e^{-i\theta Z/2}$ | Z축 θ 회전 |

---

## 4. 수학적 기초 (Mathematical Foundations)

### 4.1 양자 상태 표현

#### 4.1.1 단일 큐비트

$$|\psi\rangle = \alpha|0\rangle + \beta|1\rangle$$

정규화 조건: |α|² + |β|² = 1

**Bloch 구 표현:**

$$|\psi\rangle = \cos(\theta/2)|0\rangle + e^{i\phi}\sin(\theta/2)|1\rangle$$

#### 4.1.2 다중 큐비트

$$|\psi\rangle = \sum_{i=0}^{2^n-1} \alpha_i|i\rangle$$

**차원:** 2^n (지수적 증가)

### 4.2 노이즈 모델

#### 4.2.1 진폭 감쇠 (Amplitude Damping)

T1 완화에 의한 |1⟩ → |0⟩ 전이:

$$P_{amp} = 1 - e^{-t/T_1}$$

**Kraus 연산자:**

$$K_0 = \begin{bmatrix}1 & 0\\0 & \sqrt{1-p}\end{bmatrix}, \quad K_1 = \begin{bmatrix}0 & \sqrt{p}\\0 & 0\end{bmatrix}$$

#### 4.2.2 위상 감쇠 (Phase Damping)

T2 디코히런스에 의한 위상 정보 손실:

$$\frac{1}{T_\phi} = \frac{1}{T_2} - \frac{1}{2T_1}$$

$$P_{phase} = 1 - e^{-t/T_\phi}$$

**Kraus 연산자:**

$$K_0 = \sqrt{1-p}\cdot I, \quad K_1 = \sqrt{p}\cdot Z$$

#### 4.2.3 탈분극 채널 (Depolarizing Channel)

게이트 에러를 모델링하는 대칭적 노이즈:

$$\varepsilon(\rho) = (1-p)\rho + \frac{p}{3}(X\rho X + Y\rho Y + Z\rho Z)$$

### 4.3 피델리티 추정 모델

#### 4.3.1 기본 모델 (v1.0)

$$F_{circuit} \approx \prod_g (1 - \varepsilon_g) \times \exp\left(-\sum_g \frac{t_g}{T_1}\right)$$

#### 4.3.2 개선 모델 (v2.0)

$$F_{circuit} \approx \prod_g (1 - \varepsilon_g) \times \exp\left(-\sum_g \frac{t_g}{T_1}\right) \times \exp\left(-\sum_g \frac{t_g}{T_\phi}\right) \times (1 - \varepsilon_{ro})^{n_m}$$

여기서:
- $\varepsilon_g$: 게이트 에러율 (1Q/2Q 구분)
- $t_g$: 게이트 실행 시간
- $T_1$: 에너지 완화 시간
- $T_\phi$: 순수 위상 완화 시간
- $\varepsilon_{ro}$: 읽기 에러율
- $n_m$: 측정 큐비트 수

> **주의:** 이 모델은 **상대적 비교용 휴리스틱**입니다. 실제 피델리티와의 상관관계는 하드웨어 검증을 통해 보정 필요.

#### 4.3.3 모델 한계 (Known Limitations)

현재 모델에서 고려하지 않는 요소:
- Crosstalk (인접 큐비트 간섭) - v2.1 예정
- 비마르코프 노이즈 (Non-Markovian effects)
- SPAM (State Preparation And Measurement) 상관관계
- 게이트 간 시간 상관관계

### 4.4 게이트 교환 법칙

두 게이트 A, B가 교환 가능한 조건:

$$[A, B] = AB - BA = 0$$

**실용적 조건:**
- 서로 다른 큐비트에 작용하는 게이트는 항상 교환 가능
- 같은 큐비트의 대각 게이트(Z, S, T, Rz)는 교환 가능
- 같은 축의 회전 게이트는 교환 가능

---

## 5. 알고리즘 상세 (Algorithm Details)

### 5.1 DriftScanner 알고리즘

#### 5.1.1 개요

DriftScanner는 노이즈 프로파일을 수집하고 드리프트를 감지합니다.

> **중요:** DriftScanner는 T1/T2를 직접 측정하지 않습니다. 데이터 소스(시뮬레이터 파라미터 또는 캘리브레이션 API)에서 값을 **참조**합니다.

#### 5.1.2 동작 모드

```
SimulatorMode:
  - NoiseModel에서 T1/T2 파라미터 직접 읽기
  - 즉시 반환 (μs 단위)

CalibrationMode (v2.0):
  - Backend.properties() API 호출
  - 캘리브레이션 timestamp 확인
  - 캐시된 값 반환 (stale 체크)

HistoricalMode:
  - 저장된 캘리브레이션 기록 조회
  - 시계열 분석으로 드리프트 예측
```

#### 5.1.3 드리프트 감지

```
drift_score = |current_t1 - baseline_t1| / baseline_t1
            + |current_t2 - baseline_t2| / baseline_t2

IF drift_score > threshold:
    drift_detected = true
    trigger_reoptimization()
```

**설계 파라미터:**
- `threshold`: 0.1 (10% 변화 시 드리프트 감지) - 튜닝 가능
- `baseline_window`: 최근 10개 캘리브레이션 평균

### 5.2 GateReorder 알고리즘

#### 5.2.1 BFS 기반 변종 생성

```
INPUT: circuit, max_depth, max_variants
OUTPUT: List<CircuitVariant>

1. INITIALIZE queue = [circuit]
2. INITIALIZE visited = HashSet<circuit_hash>
3. WHILE queue.not_empty AND variants.len < max_variants:
   a. current = queue.pop_front()
   b. pairs = find_adjacent_commuting_pairs(current)
   c. FOR each (i, j) in pairs:
      new_circuit = swap_gates(current, i, j)
      hash = circuit_hash(new_circuit)
      IF hash NOT IN visited AND depth < max_depth:
         visited.add(hash)
         queue.push(new_circuit)
         variants.push(new_circuit)
4. RETURN variants
```

#### 5.2.2 Beam Search 변형 (v2.0)

대규모 회로를 위한 확장성 개선:

```
INPUT: circuit, beam_width, max_iterations
OUTPUT: best_variant

1. beam = [(circuit, score(circuit))]
2. FOR iter = 1 TO max_iterations:
   candidates = []
   FOR (circ, _) in beam:
     FOR swap in get_swaps(circ):
       new_circ = apply_swap(circ, swap)
       candidates.push((new_circ, score(new_circ)))
   beam = top_k(candidates, beam_width)
3. RETURN beam[0]
```

#### 5.2.3 복잡도 분석

| 알고리즘 | 시간 복잡도 | 공간 복잡도 | 적합한 회로 |
|----------|------------|------------|------------|
| BFS | O(V × E) | O(V) | <50 gates |
| Beam Search | O(k × n × b) | O(b) | <500 gates |

### 5.3 LiveRewirer 최적화 알고리즘

#### 5.3.1 스코어링 함수

```
score_variant(circuit, noise_profile, hardware):
  fidelity = estimate_fidelity(circuit, noise_profile)
  
  // 하드웨어 연결성 패널티
  violations = count_connectivity_violations(circuit, hardware)
  connectivity_penalty = 0.9 ^ violations
  
  RETURN fidelity × connectivity_penalty
```

> **참고:** score는 상대적 ranking용이며, 절대적 피델리티 예측값이 아닙니다.

#### 5.3.2 최적화 파이프라인

```
optimize(circuit, noise_profile):
  IF circuit.depth < 50:
    variants = bfs_reorder(circuit)
  ELSE:
    variants = beam_search_reorder(circuit)
  
  scored = [(v, score_variant(v, noise_profile)) for v in variants]
  sorted = sort_by_score_desc(scored)
  
  RETURN sorted[0].variant
```

### 5.4 StateVector 시뮬레이션

#### 5.4.1 게이트 적용

단일 큐비트 게이트 적용 (비트 마스킹):

```
apply_single_qubit_gate(sv, gate, target):
  FOR i = 0 TO 2^n - 1:
    IF bit(i, target) == 0:
      j = i | (1 << target)
      [sv[i], sv[j]] = gate × [sv[i], sv[j]]
```

**복잡도:** O(2^n)

#### 5.4.2 측정 (Born Rule)

```
measure(sv, shots):
  probs = [|α_i|² for α_i in sv]
  results = {}
  FOR i = 1 TO shots:
    outcome = sample_from(probs)
    results[outcome] += 1
  RETURN results
```

---

## 6. 시스템 아키텍처 (System Architecture)

### 6.1 모듈 구조 (현재)

```
qns-mvp/
├── crates/
│   ├── qns_core        # 핵심 타입: Gate, NoiseVector, CircuitGenome, HardwareProfile
│   ├── qns_profiler    # 노이즈 프로파일링: DriftScanner
│   ├── qns_rewire      # 회로 최적화: GateReorder, LiveRewirer, Router
│   ├── qns_simulator   # 양자 시뮬레이션: StateVectorSimulator, NoisySimulator
│   ├── qns_cli         # CLI 및 통합: Pipeline, Server
│   ├── qns_qasm        # OpenQASM 파서: Parser, AST, Builder
│   ├── qns_noise       # 노이즈 채널: NoiseChannel, NoiseModel
│   ├── qns_tensor      # 텐서 네트워크: TensorNetwork, MPS
│   ├── qns_python      # Python 바인딩: PyO3 모듈
│   └── qns-python      # Python 통합 패키지
├── docs/               # 문서
├── scripts/            # 빌드/유틸리티 스크립트
└── .github/            # CI/CD
```

### 6.2 현재 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    QNS Current Architecture                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐     │
│  │  qns_core   │    │qns_profiler │    │ qns_rewire  │     │
│  │  [✅ 완료]  │    │  [✅ 완료]  │    │ [⚠️ 스켈레톤]│     │
│  └─────────────┘    └─────────────┘    └─────────────┘     │
│         │                  │                  │            │
│         └──────────────────┴──────────────────┘            │
│                           │                                │
│  ┌─────────────┐    ┌─────┴─────┐    ┌─────────────┐      │
│  │  qns_qasm   │    │qns_simulator│   │ qns_tensor  │      │
│  │  [✅ 완료]  │    │[⚠️ 스켈레톤]│   │  [✅ 완료]  │      │
│  └─────────────┘    └───────────┘    └─────────────┘      │
│         │                  │                  │            │
│         └──────────────────┴──────────────────┘            │
│                           │                                │
│                    ┌──────┴──────┐                         │
│                    │             │                         │
│              ┌─────┴─────┐ ┌─────┴─────┐                   │
│              │ Simulator │ │ qns_noise │                   │
│              │  Backend  │ │ [✅ 완료] │                   │
│              └───────────┘ └───────────┘                   │
│                    │                                       │
│              ┌─────┴─────────────┐                         │
│              │   qns_cli / API   │                         │
│              │   [⚠️ 스켈레톤]   │                         │
│              └───────────────────┘                         │
│                         │                                  │
│              ┌──────────┼──────────┐                       │
│              │          │          │                       │
│         ┌────┴────┐ ┌───┴────┐ ┌───┴────┐                 │
│         │   CLI   │ │  PyO3  │ │  REST  │                 │
│         │[⚠️ 진행]│ │[⚠️ 진행]││[📋 계획]│                 │
│         └─────────┘ └────────┘ └────────┘                 │
└─────────────────────────────────────────────────────────────┘
```

### 6.3 목표 아키텍처 (Hardware 연동)

```
┌─────────────────────────────────────────────────────────────┐
│                    QNS Target Architecture                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                    ┌──────┴──────┐                         │
│                    │             │                         │
│              ┌─────┴─────┐ ┌─────┴─────┐                   │
│              │ Simulator │ │ Hardware  │  ← 목표           │
│              │  Backend  │ │  Backend  │                   │
│              └───────────┘ └───────────┘                   │
│                    │             │                         │
│                    │       ┌─────┴─────┐                   │
│                    │       │  Qiskit   │  ← 목표           │
│                    │       │  Bridge   │                   │
│                    │       └───────────┘                   │
│                    │             │                         │
│              ┌─────┴─────────────┴─────┐                   │
│              │      qns_cli / API      │                   │
│              └─────────────────────────┘                   │
│                           │                                │
│              ┌────────────┼────────────┐                   │
│              │            │            │                   │
│         ┌────┴────┐ ┌─────┴─────┐ ┌────┴────┐             │
│         │   CLI   │ │   PyO3    │ │  REST   │  ← 목표     │
│         │         │ │  Binding  │ │   API   │             │
│         └─────────┘ └───────────┘ └─────────┘             │
└─────────────────────────────────────────────────────────────┘
```

### 6.4 HardwareBackend Trait (설계)

```rust
/// 하드웨어 추상화 인터페이스
pub trait HardwareBackend: Send + Sync {
    /// 백엔드 이름
    fn name(&self) -> &str;
    
    /// 캘리브레이션 데이터 조회
    fn get_calibration(&self) -> Result<CalibrationData>;
    
    /// 회로 실행
    fn execute(&self, circuit: &CircuitGenome, shots: usize) 
        -> Result<ExecutionResult>;
    
    /// 토폴로지 조회
    fn get_topology(&self) -> Result<HardwareProfile>;
    
    /// 캘리브레이션 타임스탬프
    fn calibration_timestamp(&self) -> Option<DateTime<Utc>>;
}

/// 시뮬레이터 백엔드 (v1.0 구현)
pub struct SimulatorBackend {
    noise_model: NoiseModel,
    simulator: StateVectorSimulator,
}

/// IBM Quantum 백엔드 (v2.0 계획)
pub struct QiskitBackend {
    service: IBMQService,
    backend_name: String,
    session: Option<Session>,
}
```

### 6.5 데이터 흐름

```
┌─────────────────────────────────────────────────────────────┐
│                    QNS Pipeline                             │
├─────────────────────────────────────────────────────────────┤
│  Circuit Input                                              │
│       │                                                     │
│       ▼                                                     │
│  ┌─────────────┐                                           │
│  │ DriftScanner │───▶ NoiseVector                          │
│  │ (참조 모드)  │     (T1, T2, gate_errors, readout_error) │
│  └─────────────┘                                           │
│       │                                                     │
│       ▼                                                     │
│  ┌─────────────┐                                           │
│  │ LiveRewirer  │───▶ Optimized Circuit                    │
│  │              │     (score 기반 선택)                     │
│  └─────────────┘                                           │
│       │                                                     │
│       ▼                                                     │
│  ┌─────────────┐                                           │
│  │   Backend   │───▶ Execution Result                      │
│  │ (Sim/HW)    │                                           │
│  └─────────────┘                                           │
│       │                                                     │
│       ▼                                                     │
│  Pipeline Result (measurements, timing, metadata)          │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. 예시 코드 (Code Examples)

### 7.1 기본 사용법

```rust
use qns_core::prelude::*;
use qns_profiler::DriftScanner;
use qns_rewire::LiveRewirer;

// 1. 회로 생성
let mut circuit = CircuitGenome::new(3);
circuit.add_gate(Gate::H(0))?;
circuit.add_gate(Gate::CNOT(0, 1))?;
circuit.add_gate(Gate::CNOT(1, 2))?;

// 2. 노이즈 프로파일 참조 (시뮬레이터 모드)
let mut scanner = DriftScanner::simulator_mode();
let noise = scanner.get_profile(0)?;

// 3. 회로 최적화
let mut rewirer = LiveRewirer::new();
rewirer.load(circuit)?;
let optimized = rewirer.optimize(&noise, 50)?;
```

### 7.2 CLI 사용법

```bash
# 노이즈 프로파일 조회 (시뮬레이터)
qns profile -q 0,1,2

# 회로 최적화
qns optimize -q 5 -g 20 -m 50 -o result.json

# 시뮬레이션
qns simulate -q 3 -c bell -s 1000

# 벤치마크
qns benchmark -q 5 -g 20 -i 100
```

### 7.3 NoiseVector 확장 (v2.0)

```rust
/// 확장된 노이즈 벡터
pub struct NoiseVector {
    pub qubit_id: usize,
    
    // 기존 필드
    pub t1_mean: f64,      // μs
    pub t2_mean: f64,      // μs
    pub t1_std: f64,
    pub t2_std: f64,
    
    // v2.0 추가 필드
    pub gate_error_1q: f64,     // 단일 큐비트 게이트 에러
    pub gate_error_2q: f64,     // 2큐비트 게이트 에러
    pub readout_error: f64,     // 측정 에러
    pub frequency: Option<f64>, // 큐비트 주파수 (GHz)
    
    // 메타데이터
    pub timestamp: Option<DateTime<Utc>>,
    pub source: NoiseSource,  // Simulator | Calibration | Historical
}
```

---

## 8. 기술 스택 (Technology Stack)

### 8.1 핵심 기술

| 카테고리 | 기술 | 선정 이유 |
|----------|------|-----------|
| 언어 | Rust 1.75+ | 메모리 안전성, 성능 |
| 빌드 | Cargo Workspace | 모놀리포 멀티크레이트 |
| 수학 | num-complex, ndarray | 복소수, N차원 배열 |
| 병렬화 | rayon | 데이터 병렬 처리 |
| CLI | clap | 명령줄 인터페이스 |
| 직렬화 | serde, serde_json | 설정/결과 저장 |
| 벤치마크 | criterion | 통계 기반 측정 |

### 8.2 v2.0 추가 기술

| 카테고리 | 기술 | 용도 |
|----------|------|------|
| Python 바인딩 | PyO3 | Qiskit 생태계 통합 |
| 비동기 | tokio | 하드웨어 API 호출 |
| HTTP | reqwest | REST API 클라이언트 |
| 캐싱 | dashmap | 캘리브레이션 캐시 |

---

## 9. 성능 벤치마크 (Performance Benchmarks)

### 9.1 측정 환경

> **중요:** 모든 벤치마크는 아래 환경에서 **시뮬레이터 기준**으로 측정되었습니다.

| 항목 | 값 |
|------|-----|
| **CPU** | AMD Ryzen 9 / Intel i7 동급 |
| **메모리** | 16GB DDR4 |
| **Rust** | 1.75+ (release build) |
| **최적화** | `-O3`, LTO enabled |
| **측정 도구** | Criterion 0.5 |
| **샘플 수** | 100 iterations |

### 9.2 성능 결과 (시뮬레이터 기준)

| 컴포넌트 | 조건 | 측정값 | 비고 |
|----------|------|--------|------|
| Full Pipeline | 5q, 20gates | ~95 μs | 시뮬레이터 |
| DriftScanner | 5 qubits | ~21 μs | 파라미터 참조 |
| LiveRewirer | 50 variants | ~62 μs | BFS |
| Simulator Execute | 5q, 20gates | ~1.4 μs | StateVector |
| Measure | 5q, 1000shots | ~180 μs | 확률 샘플링 |

### 9.3 스케일링 (시뮬레이터)

| 큐비트 수 | 상태벡터 크기 | 메모리 | Execute (20g) |
|-----------|--------------|--------|---------------|
| 5 | 32 | 512 B | ~1.4 μs |
| 10 | 1,024 | 16 KB | ~45 μs |
| 15 | 32,768 | 512 KB | ~1.5 ms |
| 20 | 1,048,576 | 16 MB | ~50 ms |
| 25 | 33,554,432 | 512 MB | ~2 s |
| 30 | 1,073,741,824 | 16 GB | 분산 필요 |

### 9.4 하드웨어 연동 시 예상 (v2.0)

> **주의:** 아래는 **추정치**이며, 실제 측정은 v2.0에서 수행 예정입니다.

| 단계 | 예상 시간 | 병목 |
|------|----------|------|
| 캘리브레이션 조회 | 100-500ms | API 레이턴시 |
| 회로 최적화 | <100ms | 로컬 연산 |
| Job 제출 | 200-1000ms | 네트워크 |
| 큐 대기 | 0-수시간 | 하드웨어 가용성 |
| 실행 | 회로 의존 | 하드웨어 |
| 결과 수신 | 100-500ms | 네트워크 |

**결론:** 하드웨어 연동 시 "100ms 실시간 적응"은 불가능. **Job-to-Job 최적화** (다음 Job 제출 전 최적화)가 현실적 목표.

---

## 10. 상용화 전략 (Commercialization Strategy)

### 10.1 시장 분석

| 항목 | 내용 | 출처 |
|------|------|------|
| 글로벌 양자 SW 시장 | 2025년 ~$1-3B | Gartner, McKinsey (추정) |
| CAGR | 30-40% | 복수 리포트 평균 |
| 노이즈 관리 세그먼트 | 전체의 10-15% | 내부 추정 |

> **참고:** 정확한 TAM 산정을 위해 전문 시장 조사 필요

### 10.2 포지셔닝

**v1.0:** 오픈소스 시뮬레이터 기반 연구 도구
**v2.0:** 하드웨어 연동 회로 최적화 플랫폼

### 10.3 경쟁 분석

| 경쟁사 | 접근법 | QNS 차별점 |
|--------|--------|------------|
| IBM Qiskit Transpiler | 정적 최적화 | 노이즈 프로파일 기반 동적 선택 |
| Q-CTRL Fire Opal | 폐쇄형 SaaS | 오픈소스 + 커스터마이징 |
| tket | 컴파일러 중심 | 런타임 적응 (v2.0) |

### 10.4 비즈니스 모델 (계획)

| 티어 | 가격 | 대상 | 기능 |
|------|------|------|------|
| Open Source | 무료 | 연구자 | Core 기능 |
| Professional | TBD | 기업 | 하드웨어 연동 + 지원 |
| Enterprise | TBD | 대기업 | 커스텀 + SLA |

> **참고:** 가격 정책은 v2.0 하드웨어 검증 후 확정

---

## 11. 로드맵 (Roadmap)

### 11.1 v0.1.0 (현재) - 기반 구축

**완료:**

- ✅ 핵심 타입 및 회로 표현 (qns_core)
- ✅ DriftScanner 노이즈 프로파일링 (qns_profiler)
- ✅ OpenQASM 파서 (qns_qasm)
- ✅ 노이즈 채널 정의 (qns_noise)
- ✅ 텐서 네트워크 MPS 구현 (qns_tensor)
- ✅ CI/CD 파이프라인 (.github/workflows)
- ✅ 워크스페이스 구조 (10개 크레이트)

**진행 중:**

- 🔄 StateVector 시뮬레이터 핵심 로직 (qns_simulator)
- 🔄 LiveRewirer/GateReorder 알고리즘 (qns_rewire)
- 🔄 CLI 파이프라인 (qns_cli)
- 🔄 PyO3 Python 바인딩 (qns_python)

### 11.2 v0.2.0 (다음 목표) - 핵심 기능 완성

- 📋 StateVector 게이트 적용 로직 완성
- 📋 Born 규칙 측정 구현
- 📋 BFS/Beam Search 회로 최적화
- 📋 CLI optimize/simulate 커맨드
- 📋 기본 Python API

### 11.3 v0.3.0 - 통합 및 테스트

- 📋 전체 파이프라인 통합 테스트
- 📋 벤치마크 스위트 확장
- 📋 API 문서화
- 📋 예제 회로 및 튜토리얼

### 11.4 v1.0.0 - 프로덕션 준비

- 📋 HardwareBackend trait 구현
- 📋 IBM Quantum 연동 (Qiskit Runtime)
- 📋 실 하드웨어 벤치마크
- 📋 Job-to-Job 최적화

### 11.5 v2.0.0 (장기) - 확장

- 📋 Crosstalk 모델
- 📋 ZNE (Zero-Noise Extrapolation) 통합
- 📋 다중 백엔드 (IonQ, Rigetti)
- 📋 클라우드 서비스

---

## 부록 (Appendix)

### A. 프로젝트 구조

```
qns-mvp/
├── Cargo.toml              # 워크스페이스 설정
├── crates/
│   ├── qns_core/           # 핵심 타입 (Gate, NoiseVector, CircuitGenome)
│   │   └── src/
│   │       ├── types/      # 타입 정의
│   │       ├── physics.rs  # 물리 상수
│   │       ├── config.rs   # 설정
│   │       └── backend.rs  # HardwareBackend trait
│   ├── qns_profiler/       # 노이즈 프로파일링
│   │   └── src/drift_scan/ # DriftScanner
│   ├── qns_rewire/         # 회로 최적화
│   │   └── src/
│   │       ├── live_rewirer/
│   │       ├── gate_reorder/
│   │       └── router/
│   ├── qns_simulator/      # 양자 시뮬레이션
│   │   └── src/
│   │       ├── state_vector/
│   │       ├── noise.rs
│   │       └── noisy.rs
│   ├── qns_cli/            # CLI 및 통합
│   │   └── src/
│   │       ├── pipeline.rs
│   │       └── server.rs
│   ├── qns_qasm/           # OpenQASM 파서
│   │   └── src/
│   │       ├── parser.rs
│   │       ├── ast.rs
│   │       └── builder.rs
│   ├── qns_noise/          # 노이즈 채널
│   │   └── src/channels.rs
│   ├── qns_tensor/         # 텐서 네트워크 (MPS)
│   ├── qns_python/         # Python 바인딩 (PyO3)
│   │   ├── src/
│   │   └── python/qns/
│   └── qns-python/         # Python 통합 패키지
├── docs/                   # 문서
├── .github/workflows/      # CI/CD
├── scripts/                # 유틸리티 스크립트
├── .rustfmt.toml          # 코드 포맷팅
└── clippy.toml            # 린트 설정
```

### B. 테스트 현황

| 크레이트 | Unit | Doc | Integration | 합계 |
|----------|------|-----|-------------|------|
| qns_core | 46 | 4 | - | 50 |
| qns_profiler | 29 | 1 | - | 30 |
| qns_rewire | 25 | 3 | - | 28 |
| qns_simulator | 39 | 5 | - | 44 |
| qns_cli | 7 | 2 | 17 | 26 |
| **합계** | **146** | **15** | **17** | **178** |

### C. 참고 문헌

1. Nielsen, M. A., & Chuang, I. L. (2010). *Quantum Computation and Quantum Information.*
2. Preskill, J. (2018). Quantum Computing in the NISQ era and beyond. *Quantum 2*, 79.
3. Krantz, P., et al. (2019). A quantum engineer's guide to superconducting qubits. *APR 6(2)*.
4. IBM Quantum. (2024). Qiskit Documentation. https://qiskit.org/documentation/

### D. 라이선스

QNS는 듀얼 라이선스로 제공됩니다:

- MIT License
- Apache License 2.0

상업적 사용, 수정, 배포가 자유롭습니다.

### E. 변경 이력

| 버전 | 날짜 | 주요 변경 |
|------|------|----------|
| v1.0 | 2025-11-27 | 초기 버전 |
| v2.0 | 2025-11-27 | AI 평가 반영, 표현 수정, 모델 개선 |
| v2.1 | 2025-12-09 | 프로젝트 현황 반영, 구조 업데이트 |

**주요 변경 사항 (v2.0):**

- "실시간 T1/T2 측정" → "캘리브레이션 데이터 참조"
- "100ms 적응" → "로컬 파이프라인 기준" 명시
- 피델리티 모델에 T2 항 추가
- HardwareBackend trait 설계 추가
- 벤치마크 환경 정보 명시
- 메모리 추정치 수정 (25q: 512GB → 512MB)
- "현재 상태 vs 목표" 섹션 추가

**주요 변경 사항 (v2.1):**

- 프로젝트 구조 10개 크레이트로 확장 반영
- 모듈별 구현 현황 테이블 추가
- 현재 아키텍처 다이어그램 추가 (완료/스켈레톤 상태 표시)
- 로드맵 현실화 (v0.1.0 Beta 기준)
- 부록 프로젝트 구조 상세화
- qns_qasm, qns_noise, qns_tensor, qns_python 모듈 추가 반영

---

*— End of Document —*

*Copyright © 2025 Jung Wook Yang. Licensed under MIT/Apache 2.0.*
